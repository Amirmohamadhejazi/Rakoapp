export  const   ToggleModal=(data)=>({
    type : 'ToggleModal',
    Modal : data,
});
export  const   Show=(data)=>({
    type : 'Show',
    Modal : data,
});
export  const   Hide=(data)=>({
    type : 'Hide',
    Modal : data,
});